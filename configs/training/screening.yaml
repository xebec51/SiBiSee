run_name: screening-yolov8s-seed0
output_dir: artifacts/runs
model: yolov8s.pt
train_args:
  data: ${SIBISEE_DATASET_YAML}
  epochs: 25
  imgsz: 640
  batch: 16
  optimizer: SGD
  lr0: 0.01
  lrf: 0.01
  seed: 0
  deterministic: true
  patience: 8
  device: 0
  workers: 4
  project: artifacts/runs
  name: screening-yolov8s-seed0
